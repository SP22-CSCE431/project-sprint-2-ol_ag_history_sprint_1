<% if Member.find_by(:email =>current_admin.email).admin? %>
<h1>New Event</h1>

<%= render 'form', event: @event %>

<%= link_to 'Back to Events', events_path, class:"btn btn-outline-dark btn-sm"%>
<% else %>
status: 403 'Forbidden'
<% end %>