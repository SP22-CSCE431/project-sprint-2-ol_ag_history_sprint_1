<p id="notice"><%= notice %></p>

<h1>Lineages</h1>

<table>
  <thead>
    <tr>
      <th>Member</th>
      <th>Big</th>
      <th>Little</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @lineages.each do |lineage| %>
      <tr>
        <td><%= Member.where(id: lineage.member_id).last.fname %></td>
        <td><%= Member.where(id: lineage.big).last.fname %></td>
        <td><%= Member.where(id: lineage.little).last.fname %></td>
        <td><%= link_to 'Show', lineage %></td>
        <td><%= link_to 'Edit', edit_lineage_path(lineage) %></td>
        <td><%= link_to 'Destroy', lineage, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Lineage', new_lineage_path %>
